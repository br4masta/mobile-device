<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cobasatu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CobaSatu">
        <activity
            android:name=".Test"
            android:label="@string/title_activity_test"
            android:theme="@style/Theme.CobaSatu.NoActionBar"></activity>
        <activity android:name=".UpdateBiodataActivity" />
        <activity android:name=".LihatBiodataActivity" />
        <activity android:name=".BuatBiodataActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>